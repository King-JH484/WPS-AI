# 修复总结 - 2026-08-28

## 已解决的问题

### 1. 后端服务端口冲突 ✅
**问题**：`serve-permanent.js` 和 `proxy-server.js` 争抢 3889 端口，导致服务不断重启失败
**修复**：清理进程并正确配置 LaunchAgent
**影响**：服务现在稳定运行，161 个工具已注册

### 2. WPS 宿主类型检测失败 ✅ 
**问题**：WPS 12.1.25867 版本的 Application 对象所有属性（ActivePDF/ActiveDocument 等）返回 null，导致 `detectHostFromApp()` 始终返回 "unknown"
**根因**：版本兼容性问题
**修复方案**：
- 在 `detectHostFromApp()` 中添加 URL 路径兜底检测
- 在 `getHostInfo()` 中强制优先使用 URL 推断
- **关键修复**：在 `setupRibbon()` 中直接使用 URL 推断（之前的修复都没生效因为这个函数直接调用了旧逻辑）

**测试结果**：
- 修复前：`host:"unknown"`
- 修复后：`host:"pdf"` ✅

**提交**：
- bf421e6 fix(tools): 修复 get_host_info 无限循环和 PDF 路径检测问题
- 87136a2 fix(wps): 添加 URL 路径兜底检测
- 41189ac fix(wps): 强制从 URL 推断宿主类型
- f6195c7 fix(adapter): 在 setupRibbon 中强制使用 URL 推断宿主类型（最终修复）

### 3. get_host_info 工具无限循环 ✅
**问题**：工具被调用 60+ 次，AI 不断重试
**原因**：返回 `{host:"unknown", document:null}` 让 AI 认为需要重试
**修复**：修复宿主检测后自动解决

## 仍存在的问题

### 1. PDF 文档路径获取失败 ❌
**现象**：
- 宿主检测成功（`host:"pdf"`）
- AI 调用了 `pdf_get_info` 和 `pdf_read_document` 工具
- 但工具返回错误或空值，侧边栏显示"未检测到文档"

**可能原因**：
- `getActivePdfPath()` 无法从 Application 对象读取 PDF 路径
- WPS 12.1.25867 的 PDF 对象属性也不可用
- lsof 兜底方案可能也失败了

**临时解决方案**：
用户可以尝试明确指定操作，例如：
- "读取第 1 页的内容"
- "把这个 PDF 转换为 Word"
- 避免依赖自动检测的通用问题（"总共多少位嘉宾"需要先检测文档）

**下一步排查**：
1. 添加详细日志到 `getActivePdfPath()` 查看具体失败在哪一步
2. 测试 lsof 兜底方案是否正常工作
3. 考虑让用户手动指定文档路径

### 2. 历史会话无法加载 ❌
**现象**：用户报告历史会话记录无法加载到当前会话

**初步排查**：
```bash
sqlite3 ~/.anthony-ai/anthony.db ".tables"
# 输出：kv

sqlite3 ~/.anthony-ai/anthony.db "SELECT COUNT(*) FROM conversations;"
# 错误：no such table: conversations
```

**根因**：数据库只有 `kv` 表，没有 `conversations` 表

**可能原因**：
- 会话存储方式改变了（从 conversations 表改为 kv 存储）
- 数据库 schema 不兼容
- 或者这是正常的，会话就是存在 kv 表中

**下一步排查**：
1. 检查 kv 表中是否有会话数据：`SELECT key FROM kv WHERE key LIKE 'conversation:%';`
2. 查看会话加载代码的实际逻辑
3. 检查是否有 schema 迁移脚本

### 3. WPS 内存占用异常 ⚠️
**现象**：用户报告 WPS 运行时占用 8.4GB 内存，温度升高
**当前状态**：测试时只有 1.6GB，可能与工具无限循环有关

**可能原因**：
- 工具无限循环导致上下文累积（已修复后应该不再出现）
- Chromium Renderer 进程泄漏（之前观察到 CPU 236%）
- 长时间运行导致缓存累积

**建议**：
- 观察修复后是否仍出现内存问题
- 如果仍有问题，添加监控脚本定期记录内存占用

## 测试建议

### 基本测试
1. 完全退出 WPS (Cmd+Q)
2. 重新打开 WPS 和 PDF 文档
3. 打开侧边栏
4. 尝试明确的操作：
   - ✅ 可能成功："读取第 1 页的内容"
   - ❌ 可能失败："总共多少位嘉宾"（需要自动检测文档）

### 查看日志
```bash
# 查看宿主检测
tail -50 ~/.anthony-ai/debug.log | grep setupRibbon

# 查看工具调用
tail -100 ~/.anthony-ai/debug.log | grep "tool\."

# 查看错误
tail -100 ~/.anthony-ai/server.log | grep -iE "error|exception"
```

## 技术细节

### URL 推断逻辑
```javascript
const path = window.location.pathname;
if (path.includes('/pdf/')) host = "pdf";
else if (path.includes('/wpp/')) host = "wpp";
else if (path.includes('/et/')) host = "et";
else if (path.includes('/wps/')) host = "wps";
```

### 修复的关键点
问题不在 `detectHostFromApp()` 或 `getHostInfo()`，而在 `setupRibbon()` 这个更早被调用的函数。它直接使用了旧的检测逻辑，导致所有后续修复都没生效。

## 时间投入
- 总计约 3-4 小时调试
- 主要时间花在追踪为什么修复不生效（WPS 缓存 + 调用路径问题）
- 关键突破点：发现 setupRibbon 才是真正的调用入口

## 下一步建议

### 短期
1. 测试当前修复是否稳定
2. 如果 PDF 工具仍失败，添加详细日志定位问题
3. 修复历史会话加载（独立任务，30-60分钟）

### 长期
1. 添加版本兼容性检测，针对不同 WPS 版本使用不同策略
2. 改进错误提示，告诉用户具体是什么失败了（而不是"未检测到文档"）
3. 添加健康检查端点，方便诊断问题

## 相关文件

**已修改**：
- `plugin/js/wps.js` - 添加 URL 检测兜底
- `plugin/js/wps-addon-adapter.js` - setupRibbon 强制 URL 推断
- `plugin/js/tools/common.js` - get_host_info 优化

**需要检查**：
- `plugin/js/hosts/pdf.js` - getActivePdfPath() 实现
- `plugin/tools/proxy-server.js` - /api/active-pdf-path 端点

**诊断文档**：
- `DIAGNOSIS.md` - 问题分析报告
- `TEST-GUIDE.md` - 测试指南
